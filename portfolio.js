// Logout function to go back to the login page
function logout() {
    // Optional: clear any active session storage if you use it later
    window.location.href = "login.html"; // Make sure your login file name matches
}